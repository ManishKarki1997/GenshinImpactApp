const findWeaponsForAscensionMaterial = (ascMaterials) => {
  console.log(ascMaterials);
  // const weapons = WeaponsList.filter((w) =>
  // w.ascensionMaterials.some(
  //   (y) =>
  //     y.type === "WEAPON_ASCENSION_BOSS_MATERIAL" &&
  //     y.name === selectedWeaponAscensionMaterialName
  // )
  // );
};

export default findWeaponsForAscensionMaterial;
